<%= form_with(model: can, local: true) do |form| %>
  <% if can.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(can.errors.count, "error") %> prohibited this can from being saved:</h2>

      <ul>
      <% can.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :peso %>
    <%= form.text_field :peso, id: :can_peso %>
  </div>

  <div class="field">
    <%= form.label :edad %>
    <%= form.text_field :edad, id: :can_edad %>
  </div>

  <div class="field">
    <%= form.label :integer %>
    <%= form.text_field :integer, id: :can_integer %>
  </div>

  <div class="field">
    <%= form.label :rasa %>
    <%= form.text_field :rasa, id: :can_rasa %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :can_color %>
  </div>

  <div class="field">
    <%= form.label :precio %>
    <%= form.number_field :precio, id: :can_precio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
